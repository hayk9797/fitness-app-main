<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>

<body>
  <nav class="navbar">
    <div class="nav-container">
      <a href="/" class="nav-brand">🏋️ FitnessApp</a>
      <div class="nav-links">
        <% if (currentUser) { %>
          <a href="/">Strona główna</a>
          <a href="/workouts/new">Nowy trening</a>
          <a href="/logout">Wyloguj się</a>
        <% } else { %>
          <a href="/login">Zaloguj się</a>
          <a href="/register">Zarejestruj się</a>
        <% } %>
      </div>
    </div>
  </nav>

  <h1>Edytuj trening</h1>

  <form action="/workouts/<%= workout.id %>?_method=PUT" method="POST">
    <label>Tytuł:</label>
    <input type="text" name="title" value="<%= workout.title %>" required>

    <label>Typ:</label>
    <input type="text" name="type" value="<%= workout.type %>" required>

    <label>Czas trwania (minuty):</label>
    <input type="number" name="duration" value="<%= workout.duration %>" required>

    <label>Intensywność:</label>
    <select name="intensity" required>
      <option value="Düşük" <%= workout.intensity === "Düşük" ? "selected" : "" %>>Niska</option>
      <option value="Orta" <%= workout.intensity === "Orta" ? "selected" : "" %>>Średnia</option>
      <option value="Yüksek" <%= workout.intensity === "Yüksek" ? "selected" : "" %>>Wysoka</option>
    </select>

    <button type="submit">Aktualizuj</button>
  </form>

  <a href="/">Powrót do strony głównej</a>
</body>
</html>
